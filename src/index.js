let gameStarted = false;

//Start the game when the user presses "Let's Start"
document.getElementById("guess_button").addEventListener("click", function (event) {
    window.alert("Get ready!")
    gameStarted = true;
 });